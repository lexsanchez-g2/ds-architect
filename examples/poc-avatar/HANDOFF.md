# Apollo v2 Avatar — Handoff

> Generated by ds-architect@v3 · Source: Apollo v2 node `17100:29935` · PoC v0.3.0

## What this is

Lossless bundle for Apollo v2 Avatar atom. Fifth atom PoC — completes Phase C atom batch. First bundle with raster image fill (in addition to Lucide SVG icon).

## Install + use

```bash
cp -r examples/poc-avatar ~/.claude/skills/apollo-v2-avatar-bundle-poc/
```

Prompt: *"Render Apollo v2 Avatar type=Fallback size=default."*

## Identity

- Universally circular (`rounded-full` 9999px) and square (width = height per size)
- 5 sizes: xs (20) / sm (24) / default (32) / lg (40) / xl (48)
- 3 mutually-exclusive Types: Image (raster), Fallback (initials), Icon (Lucide Plus glyph)
- Optional Badge slot (10×10 notification dot, bottom-right, constant size)
- NO state axis — Avatar is purely presentational
- Container fill universally `base/muted` (#f5f5f5)

## Reference

- `references/tokens.md` — 17 tokens, 6 NEW (w-5, w-10, w-12, xs text scale + compound)
- `references/components.md` — 15-cell matrix + binding tables
- `references/patterns.md` — 10 patterns including width-as-height + xs-scale debut + dual-asset pipeline

## Asset pipeline

First PoC with raster image. Both pipelines exercised:

| Type | Pipeline |
|---|---|
| Image | `data/images/_index.json` + `assets/images/<sha256>.png` (BUNDLE_SPEC §7.2) |
| Icon  | `data/icons/_index.json` + `assets/icons/plus.svg` (Lucide, BUNDLE_SPEC §7.1) |
| Fallback | TEXT — no asset |

## Audit findings

| ID | Severity | Source-DS action |
|---|---|---|
| F16 (NEW) | LOW | Only xl size meets WCAG 2.5.5 — fine if non-interactive |
| F17 (NEW) | LOW | No state axis — add if Avatar becomes interactive |
| F18 (NEW) | LOW | Add altText exposedProp |
| F19 (NEW) | LOW | Add imageSrc exposedProp for Type=Image |
| F-icon-choice | DOC | Type=Icon uses Plus (add) not User — design intent question |

## Provenance

`lsanchez-g2/ds-architect` (MIT). Spec `BUNDLE_SPEC@v0.3.0` (DRAFT).
