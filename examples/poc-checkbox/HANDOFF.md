# Apollo v2 Checkbox — Handoff

> Generated by ds-architect@v3 · Source: Apollo v2 node `46:112` · PoC v0.3.0

## What this is

Lossless bundle for Apollo v2 Checkbox atom. Fourth atom PoC alongside Button + Switch + Input. Validates v0.3.0 spec on a 4-axis checkbox with conditional checkmark slot.

## Quick install

```bash
cp -r examples/poc-checkbox ~/.claude/skills/apollo-v2-checkbox-bundle-poc/
```

Prompt: *"Render Apollo v2 Checkbox checked=Yes state=Default type=Default."*

## Identity

- 16×16 square box with raw 4px corners (NOT pill, NOT rounded-lg — its own thing)
- Lucide Check icon as checkmark glyph (only when checked=Yes)
- 5-state matrix including Pressed (first atom besides Button)
- NO Mixed/Indeterminate variant (F14 gap)
- Pressed = opacity-60 at box (not outer container)

## Reference

- `references/tokens.md` — 30 tokens, 4 NEW
- `references/components.md` — 40-cell matrix + binding tables
- `references/patterns.md` — 9 cross-cell + cross-atom observations

## A11y

- `role="checkbox"`
- `aria-checked="true"|"false"` (no `"mixed"` — F14)
- `aria-invalid="true"` on Invalid
- Touch target: Type=Box passes WCAG 2.5.5; Type=Default fails (40h)

## Audit findings

| ID | Severity | Action |
|---|---|---|
| F8 (carried) | MEDIUM | Standardize Disabled treatment across atoms |
| F11 (carried) | MEDIUM-doc | Document Invalid-ring-at-rest |
| F14 (NEW) | MEDIUM | Add Mixed/Indeterminate variant to Checkbox |
| F15 (NEW) | MEDIUM | Bind box radius (currently raw 4px) |

## Provenance

`lsanchez-g2/ds-architect` (MIT). Spec `BUNDLE_SPEC@v0.3.0` (DRAFT).
