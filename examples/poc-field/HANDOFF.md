# Apollo v2 Field — Handoff

> Generated by ds-architect@v3 · Source: Apollo v2 node `18684:15220` · PoC v0.3.0
> **First non-atom PoC.** Phase D molecule batch begins.

## What this is

Lossless bundle for Apollo v2 Field molecule. Sixth PoC bundle overall (5 atoms + this). Validates BUNDLE_SPEC v0.3.0 on deeply-nested composition (5+ levels) + INSTANCE_SWAP exposedProp.

## Install + use

```bash
cp -r examples/poc-field ~/.claude/skills/apollo-v2-field-bundle-poc/
```

Prompt: *"Render Apollo v2 Field orientation=Vertical dataInvalid=False with label='Email' and description='We never share'."*

## Identity

- MOLECULE — wraps an input mechanism + label + description + optional link
- 6 cells (3 axes; Responsive constrained to Under-Input only)
- INSTANCE_SWAP `inputType` prop — swap input mechanism per consumer need
- Width 433px hardcoded (audit signal)
- 5+ levels of nested composition (Field → InputGroup → KbdGroup → Kbd → Lucide arrows)
- No animation; no Disabled state at Field level (renderer connects to inner input)

## Reference

- `references/tokens.md` — 25 tokens via SP-8 deep unioning
- `references/components.md` — 6-cell matrix + composition graph
- `references/patterns.md` — 10 patterns including molecule conventions

## Audit findings

| ID | Severity | Action |
|---|---|---|
| F20 (NEW) | MEDIUM | Confirm whether dataInvalid propagates to nested InputGroup via Figma instance-overrides (use_figma plugin walk needed). If not, source-DS should add propagation. |
| F21 (NEW) | LOW | Fix #with-label doc-URL fragment (carries F12 pattern) |
| F22 (NEW) | LOW | Document Responsive orientation lacks Under-Label option (or add it) |

## v0.4.0 spec candidates surfaced

Field is the first PoC to surface composition-level spec gaps:
- **SP-10** (MEDIUM): variant-override propagation chain
- **SP-11** (LOW): nested-instance variant remapping

Both deferred to v0.4.0 — molecule batch will surface more.

## Provenance

`lsanchez-g2/ds-architect` (MIT). Spec `v0.3.0` DRAFT.
